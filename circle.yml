dependencies:
    override:
        - pip install --upgrade pip
        - pip install setuptools numpy scipy scikit-learn nibabel nilearn pandas matplotlib
        - python setup.py install

test:
    override:
        - cd examples && python plot_localizer_analysis.py

