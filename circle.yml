dependencies:
    cache_directories:
    override:
        - pip install --upgrade pip
	- pip install setuptools numpy scipy nibabel nilearn pandas matplotlib
	- python setup.py install

test:
    override:
	- cd examples && python plot_design_matrix.py

general:
    artifacts:
